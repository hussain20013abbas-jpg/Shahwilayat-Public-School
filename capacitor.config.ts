import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shahwilayat.school',
  appName: 'Shahwilayat Public School',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
